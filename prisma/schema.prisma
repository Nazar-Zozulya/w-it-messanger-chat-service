generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  // develop
  provider = "sqlite"
  // production
  // provider = "postgresql"
  url      = env("DATABASE_URL")
}
